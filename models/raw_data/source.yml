version: 2
sources:
  - name: datafeed_shared_schema
    database: "{{('DEV_DB')}}"
    schema: CUSTOMER

    tables:
      - name: RAW_CUSTOMERS
      - name: RAW_PAYMENTS
      - name: RAW_ORDERS
